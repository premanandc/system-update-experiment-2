{
  "name": "system-update-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:mutation": "stryker run",
    "test:mutation:package": "stryker run --mutate 'src/models/package.ts'",
    "test:mutation:update": "stryker run --mutate 'src/models/update.ts'",
    "test:mutation:device": "stryker run --mutate 'src/models/device.ts'",
    "test:mutation:batch": "stryker run --mutate 'src/models/batch.ts'",
    "test:mutation:plan": "stryker run --mutate 'src/models/plan.ts'",
    "test:mutation:execution": "stryker run --mutate 'src/models/execution.ts'",
    "test:mutation:model": "stryker run --mutate",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@stryker-mutator/core": "^8.7.1",
    "@stryker-mutator/jest-runner": "^8.7.1",
    "@stryker-mutator/typescript-checker": "^8.7.1",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.14.0",
    "jest": "^29.7.0",
    "jest-mock-extended": "^4.0.0-beta1",
    "prisma": "^6.5.0",
    "ts-jest": "^29.3.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0"
  }
}
